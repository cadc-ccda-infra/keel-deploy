search:
  tapServiceId: ivo://cadc.nrc.ca/sc2tap
  showAllColumnsInResults: true
  showObscoreTab: true

ingress:
  enabled: true
  className: "traefik"
  hosts:
    - host: sc2.canfar.net
      paths:
        - path: /
          pathType: Prefix
  tls: []