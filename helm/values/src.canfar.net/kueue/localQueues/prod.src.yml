apiVersion: kueue.x-k8s.io/v1beta2
kind: LocalQueue
metadata:
  namespace: canfar-src-workloads
  name: src-default
  labels:
    app.kubernetes.io/name: kueue
spec:
  clusterQueue: src
  fairSharing:
    weight: "1"
