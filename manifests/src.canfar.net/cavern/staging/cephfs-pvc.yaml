apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: canfar-src-staging-cavern-pvc
  namespace: canfar-src-staging
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1000Gi
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: cephfs-legacy-canfar-pv
