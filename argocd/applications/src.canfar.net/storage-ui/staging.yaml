apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: src-storage-ui-staging
  namespace: canfar-argocd
  labels:
    argocd.argoproj.io/domain: staging-src.canfar.net
    argocd.argoproj.io/environment: staging
    argocd.argoproj.io/service: storage-ui
spec:
  project: canfar
  sources:
    - repoURL: https://images.opencadc.org/chartrepo/client
      chart: storageui
      targetRevision: 1.4.4
      helm:
        valueFiles:
          - $values/helm/values/src.canfar.net/storage-ui/base.yaml
          - $values/helm/values/src.canfar.net/storage-ui/staging.yaml
    - repoURL: https://github.com/cadc-ccda-infra/keel-deploy.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: canfar-src-staging
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
