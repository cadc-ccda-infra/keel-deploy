apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: src-posix-mapper-staging
  namespace: canfar-argocd
  labels:
    argocd.argoproj.io/domain: staging-src.canfar.net
    argocd.argoproj.io/environment: staging
    argocd.argoproj.io/service: posix-mapper
spec:
  project: canfar
  sources:
    - repoURL: https://images.opencadc.org/chartrepo/platform
      chart: posixmapper
      targetRevision: 0.6.0
      helm:
        valueFiles:
          - $values/helm/values/src.canfar.net/posix-mapper/base.yaml
          - $values/helm/values/src.canfar.net/posix-mapper/staging.yaml
    - repoURL: https://github.com/cadc-ccda-infra/keel-deploy.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: canfar-src-staging
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
