apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cadc-reg-staging
  namespace: cadc-argocd
  labels:
    argocd.argoproj.io/domain: staging-cadc-west-01.canfar.net
    argocd.argoproj.io/environment: staging
    argocd.argoproj.io/service: reg
spec:
  project: cadc
  sources:
    - repoURL: https://images.opencadc.org/chartrepo/core
      chart: reg
      targetRevision: 1.2.1
      helm:
        valueFiles:
          - $values/helm/values/cadc-west.canfar.net/reg/base.yaml
          - $values/helm/values/cadc-west.canfar.net/reg/staging.yaml
    - repoURL: https://github.com/cadc-ccda-infra/keel-deploy.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: cadc-system-staging
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
